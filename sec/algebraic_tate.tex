% !TEX root = ../tate.tex

\section{The algebraic Tate diagonal}\label{Tatediag}

Let \(V\) be a chain complex in \(\cD(\Fp)\).

\subsection{The algebraic Tate diagonal}

\begin{definition}
	We denote by \(T_p\) the functor
	\[
	T_p \coloneqq (- \otimes \overset{p}{\cdots} \otimes -)^{t\cyc_p} \colon \mathcal D(\Fp) \to \mathcal D(\Fp).
	\]
	Explicitly, \(T_pV\) is the \(\cyc_p\)-Tate construction of the chain complex of \(\Fp[\cyc_p]\)-modules \(V^{\otimes p}\), whose \(\cyc_p\)-action is given by permuting the factors.
	\anibal{This is not right. The functor uses the diagonal functor of categories \(C \to Cx...xC\) and then the monoidal product to get back to C. Also you first land in D G-mod then back to D mod. Write this as a composition of functors with the xra{...} arrows }
\end{definition}

\begin{example}
	Taking \(V = \Fp\), we have \(T_p(\Fp) = (\Fp^{\otimes p})^{t\cyc_p} \cong \Fp^{t\cyc_p}\).\anibal{what is \(\Fp^{t\cyc_p}\)? also, do it for a general suspension of \(\Fp\)}
\end{example}

\begin{lemma}
	\(T_p\) is an exact functor.\anibal{Proof? this cannot rely on spectra as your comment below might suggest. You might be missing a standard regerence in group cohomology. as this paper should sit in the intersection of the two fields. Also, I am assuming exact funtor is defined in the Derived Categories appendix? If so, add a link}
\end{lemma}% immediate from Nikolaus-Scholze?

\begin{definition}
	An \defn{algebraic Tate diagonal} is a nonzero (\(\Fp\)-linear) natural transformation \(\id_{\cD(\Fp)} \to T_p\).\anibal{What are the morphisms in the target of the functors, that is what defines the type of maps that appear in the nat transf. This makes me think that I don't recall reading what the morphisms in the derived category are. I guess that is in the appendix}
\end{definition}

We will show that, up to scalar multiplication, there is only one algebraic Tate diagonal.
To do so we will first state the version on \(\cD(\Fp)\) of Proposition III.1.2 from [Nikolaus-Scholze].
Consider the full subcategory \(\Fun^{\mathrm{Ex}}(\cD(\Fp),\cD(\Fp)) \subset \Fun(\cD(\Fp),\cD(\Fp))\) of exact functors.

\begin{proposition}
	For any \(F \in \Fun^{\mathrm{Ex}}(\cD(\Fp),\cD(\Fp))\), evaluation at the chain complex \(\Fp\) concentrated in degree \(0\) induces an equivalence
	\[
	\mathrm{Map}_{\Fun^{\mathrm{Ex}}(\cD(\Fp),\cD(\Fp))}(\id_{\cD(\Fp)},F) \longrightarrow \Hom_{\cD(\Fp)}(\Fp,F(\Fp)).
	\]
\end{proposition}
\anibal{Proof?}

\begin{corollary}
	There is an equivalence
	\[
	\mathrm{Map}_{\Fun^{\mathrm{Ex}}(\cD(\Fp),\cD(\Fp))}(\id_{\cD(\Fp)},T_p) \longrightarrow \Hom_{\cD(\Fp)}(\Fp,T_p(\Fp)) \cong \Fp.
	\]
	Thus, the set of algebraic tate diagonals together with the zero natural transformation is a one dimensional \(\Fp\)-vector space.\anibal{No need to rewrite the diagram. What you want seems to be the conclusion after the diagram.}
\end{corollary}

% and we can characterise the algebraic Tate diagonal in the following way:
% \begin{proposition}
	% 	The algebraic Tate diagonal \(\Delta^t_p\) is the natural transformation
	% 	\[
	% 	\Delta^t_p \colon \id_{\cD(\Fp)} \to T_p
	% 	\]
	% 	that evaluated on the complex \(\Fp\) concentrated in degree \(0\) corresponds to the inclusion \(\Fp \hookrightarrow T_p(\Fp)\) given by \(1 \mapsto \widehat e_0\).
	% 	\martin{This isn't completely canonical tho, it depends on the choice of the isomorphism \(T_p(\Fp)_0 \cong \Fp\set{\widehat e_0}\).}
	% \end{proposition}

We now describe a generator of such \(\Fp\)-vector space.
Let us fix an isomorphism \(\varphi \colon H(\widehat W_p \ot_{\cyc_p} V^{\ot p}) \xra{\cong} H(T_pV)\).

\begin{definition}
	Let \(\Delta^t_p\) be the natural transformation
	\[
	\Delta^t_p \colon \id_{\cD(\Fp)} \to T_p
	\]
	which is characterised on homology by
	\[
	\begin{tikzcd}[row sep=0, column sep=small]
		(\Delta^t_pV)_* &[-20pt] \colon H(V) \arrow[r] & H(T_pV) \\
		& v \arrow[r, |->] & \varphi\left(\widehat{e}_{(1-p)|v|} \ot (v \ot \overset{p}{\cdots} \ot v)\right).
	\end{tikzcd}
	\]
\end{definition}

We will often ommit the isomorphism \(\varphi\) for clarity of notation, and denote the components simply by \(\Delta^t_p\) instead of \(\Delta^t_pV\).
Observe that \(\Delta^t_p\) is nonzero.
In fact, evaluated at \(\Fp\) corresponds to the inclusion \(\Fp \hookrightarrow T_p(\Fp)\) given by \(1 \mapsto \widehat e_0\).
To conclude that \(\Delta^t_p\) is indeed an algebraic Tate diagonal, it remains to prove its linearity.

\begin{lemma}\label{linearity}
	\(\Delta^t_p\) is \(\bF_p\)-linear.
\end{lemma}

\begin{proof}
	First we check scalar multiplicativity, let \(a \in \bF_p\) and \(x \in H_n(V)\).
	\[
	\Delta^t_p(ax) = \widehat e_{(1-p)n} \ot (ax)^{\ot p} = a^p(\widehat e_{(1-p)n} \ot x^{\ot p}) = a\Delta^t_p(x),
	\]
	where the last equality holds in \(\bF_p\) due to Fermat's little theorem.
	Now, for additivity, let \(x\) and \(y \in H_n(V)\).
	We have
	\[
	\Delta^t_p(x+y) = \widehat e_{(1-p)n} \ot (x+y)^{\ot p}.
	\]
	The term \((x+y)^{\ot p}\) is a sum of \(2^p\) terms, corresponding to the set of all possible words of length \(p\) using the letters \(x\) and \(y\).
	The cyclic group acts on this set by permutation, and there are two types of orbits: those consisting of a single element and those free with \(p\) elements.
	There are two trivial orbits with a single element, corresponding to \(x^{\otimes p}\) and \(y^{\otimes p}\) respectively.
	Let \({\bf w}_1,\dots,{\bf w}_{(2^p-2)/p}\) be representatives of the remaining orbits.
	Thus, we can write
	\[
	\widehat e_{(1-p)n} \ot (x+y)^{\ot p} = \widehat e_{(1-p)n} \ot x^{\ot p} + \widehat e_{(1-p)n} \ot y^{\ot p} + \sum_{i=1}^{(2^p-2)/p} \widehat e_{(1-p)n} \ot N{\bf w}_i,
	\]
	% 	but
	% 	\[
	% 	\widehat e_{(1-p)n} \ot_{\cyc_p} N{\bf w}_i = N\widehat e_{(1-p)n} \ot_{\cyc_p} {\bf w}_i = d\widehat e_{(1-p)n+1} \ot_{\cyc_p} {\bf w}_i = d(\widehat e_{(1-p)n+1} \ot_{\cyc_p} {\bf w}_i),
	% 	\]
	% 	where the second equality holds because \((1-p)n+1\) is an odd number for \(p\) prime (and \(d\widehat e_j = N\widehat e_{j-1}\) for any \(j\) if \(p=2\)), and the last equality holds since \(x\) and \(y\) are cycles.
	but the elements \(\widehat e_{(1-p)n} \ot N{\bf w}_i\) lie on the subcomplex of \(\widehat W_p \ot H(V)^{\ot p}\) of free orbits, which is acyclic, meaning that the term \(\sum_{i=1}^{(2^p-2)/p} \widehat e_{(1-p)n} \ot N{\bf w}_i\) vanishes, and so
	\[
	\Delta^t_p(x+y) = \Delta^t_p(x) + \Delta^t_p(y).\qedhere
	\]
\end{proof}

\begin{corollary}
	Every algebraic Tate diagonals is of the form \(a\Delta^t_p\), for \(a \in \Fp^\times\).
	This means that, up to non-zero scalar multiplication, \(\Delta^t_p\) is the only algebraic Tate diagonal, thus, we refer to it as \defn{the algebraic Tate diagonal}.\anibal{This sentence is not part of the corollary}
\end{corollary}

\begin{remark}
	Note that \(\Delta^t_p\) is not canonical, since it depends on the choice of the isomorphism \(\varphi\).
\end{remark}

\subsection{Properties of \(T_pV\)}

The image of \((\Delta^t_p)_*\) generates \(H(T_pV)\) as a \(\Fp[u,u^{-1}]\)-module with action
\[
u \Delta^t_*(v) = u(\widehat e_{(1-p)|v|} \ot v^{\ot p}) \coloneqq \widehat e_{(1-p)|v|+1} \ot v^{\ot p}.
\]
\anibal{Where is the homology of V here? I see where it is, but it took me some time, so I think it can be said better: the image of \(HV\) via ... Also, is it a free module?}
In other words, we have the following:\anibal{This is a point of debate in the comunity and I am siding with Milnor. Use period for this sentence. You can read it completely or you can link it to the first word below. i.e. replace : by .}

\begin{lemma}\label{pTate_iso}
	There is an isomorphism of free \(\Fp[u,u^{-1}]\)-modules:\anibal{No ``there is'' you are defing it explicitly}
	\[
	\begin{tikzcd}[row sep=0, column sep=small]
		\Fp[u,u^{-1}] \ot H(V) \arrow[r] & H(T_pV) \\
		\hspace*{13pt} u^k \ot v \arrow[r, |->] & u^k \Delta^t_*(v).
	\end{tikzcd}
	\]
\end{lemma}

\begin{proof}
	First, we have that
	\[
	H(T_pV) \cong H(\widehat W_p \otimes_{\cyc_p} V^{\otimes p}) \cong H(\widehat W_p \otimes_{\cyc_p} H(V)^{\otimes p}). \martin{This first step is delicate, I am not fully sure that it is true without any extra assumptions. In particular, this implies that the spectral sequence in Lemma \cref{spectral_seq} converges at the second page for the case \(M = V^{\ot p}\), which should be true.}
	\]
	Now, \(H(V)\) is a graded \(\Fp\)-vector space, so we can consider a basis \(\set{v_\alpha}_{\alpha \in A}\) for \(H(V)\).
	Since \(H(V)^{\ot p}\) is a permutation module, we can decompose it into orbits.
	There are two types of orbits, the trivial orbits with a single element and the free orbits with \(p\) elements, that is,
	\[
	H(V)^{\ot p} \cong \left(\bigoplus_{\alpha \in A} \Fp\set{v_\alpha^{\ot p}}\right) \oplus \left(\bigoplus_{\text{free orbits}} \Fp[\cyc_p]\right).
	\]
	Tensoring over \(\cyc_p\) with \(\widehat W_p\) gives
	\[
	\widehat W_p \ot_{\cyc_p} H(V)^{\ot p} \cong \left(\bigoplus_{\alpha \in A} \widehat W_p \ot_{\cyc_p} \Fp\set{v_\alpha^{\ot p}}\right) \oplus \left(\bigoplus_{\text{free orbits}} \widehat W_p\right),
	\]
	but \(\widehat W_p\) is acyclic, and \(H(\widehat W_p \ot_{\cyc_p} \Fp) \cong H(T_p\Fp) \cong \Fp[u,u^{-1}]\).
	It is thus clear that the given map is an isomorphism.
\end{proof}

\begin{lemma}\label{fibre_cofibre}
	There is an equivalence \(T_pV[-1] \simeq T_pV\) in \(\cD(\Fp)\).\anibal{no ``there is''. If the map is not constructed then just say they are isomorphic ``equivalent'' is not defined.}
\end{lemma}

\begin{proof}
	The proof of Lemma \cref{pTate_iso} works analogously for \(H(T_pV[-1])\), which means that there are isomorphisms
	\[
	H(T_pV[-1]) \cong H(\widetilde W_p \otimes_{\cyc_p} V^{\ot p}) \cong \Fp[u,u^{-1}] \ot H(V) \cong H(\widehat W_p \otimes_{\cyc_p} V^{\ot p}) \cong H(T_pV).
	\]
\end{proof}

\begin{corollary}
	\(T_pV\) fits in a long fibre-cofibre sequence % whats the appropiate name for this?
	\[
	\cdots \to (V^{\ot p})^{h\cyc_p}[-1] \to T_pV \to (V^{\ot p})_{h\cyc_p} \overset{Nm}\longrightarrow (V^{\ot p})^{h\cyc_p} \to T_pV \to \cdots.
	\]
	\anibal{I don't know about the name, but again the issue of indefinite versus definite article. Here is ``in the following''}
\end{corollary}

\subsection{Monoidality}

The identity functor \(\id_{\cD(\Fp)}\) is a lax monoidal functor: for chain complexes \(C\) and \(D\), there is a natural morphism \(\kappa\) in \(\cD(\Fp)\) that is characterised in homology by the Künneth isomorphism \martin{is this correctly formulated? Or rather \(\kappa\) is the identity, which is characterised in homology by the kunneth isomorphism? Thus the identity is monoidal, not lax monoidal. Anibal: The identity better be strong monoidal. }
\[
\kappa_* \colon H(C) \otimes H(D) \longrightarrow H(C \otimes D).
\]

The functor \(T_p\) is also a lax monoidal functor.\anibal{word functor appears too many time sin such a short sentence. Also, this should be a proposition.}
Indeed, there is a natural morphism characterised by
\[
\mu_* \circ \kappa_* \colon H\big(\widehat W_p \otimes_{\cyc_p} C^{\otimes p}\big) \otimes H\big(\widehat W_p \otimes_{\cyc_p} D^{\otimes p}\big) \longrightarrow H\big(\widehat W_p \otimes_{\cyc_p} (C \otimes D)^{\otimes p}\big),
\]
where
\[
\mu_* \colon H\big((\widehat W_p \otimes_{\cyc_p} C^{\otimes p}) \otimes (\widehat W_p \otimes_{\cyc_p} D^{\otimes p})\big) \longrightarrow H\big(\widehat W_p \otimes_{\cyc_p} (C \otimes D)^{\otimes p}\big)
\]
is induced by the assignment
\[
[(\rho^a e_i \otimes_{\cyc_p} \mathbf{c}) \otimes (\rho^b e_j \otimes_{\cyc_p} \mathbf{d})] \longmapsto [e_{i+j} \otimes_{\cyc_p} \mathrm{Sh}(\rho^{-a}\mathbf{c},\rho^{-b}\mathbf{d})],
\]
for \(\mathbf{c} = c_1 \otimes \cdots \otimes c_p \in C^{\otimes p}\) and \(\mathbf{d} = d_1 \otimes \cdots \otimes d_p \in D^{\otimes p}\).
Here \(\mathrm{Sh}\) denotes the shuffle map \(C^{\otimes p} \times D^{\otimes p} \to (C \otimes D)^{\otimes p}\) given by
\[
((c_1 \otimes \cdots \otimes c_p),(d_1 \otimes \cdots \otimes d_p)) \longmapsto (c_1 \otimes d_1) \otimes \cdots \otimes (c_p \otimes d_p).
\]

The following diagram commutes for all chain complexes \(C\) and \(D\):

\begin{center}
	\begin{tikzpicture}
		\node (a) at (-3,2) {\(H(C) \otimes H(D)\)};
		\node (b) at (4,2) {\(H(\widehat W_p \otimes_{C_p} C^{\otimes p}) \otimes H(\widehat W_p \otimes_{C_p} D^{\otimes p})\)};
		\node (c) at (-3,0) {\(H(C \otimes D)\)};
		\node (d) at (4,0) {\(H(\widehat W_p \otimes_{C_p} (C \otimes D)^{\otimes p})\)};
		%
		\draw[->] (a) -- node[above] {\((\Delta^t_p)_* \otimes (\Delta^t_p)_*\)} (b);
		\draw[->] (a) -- node[right] {\(\kappa_*\)} (c);
		\draw[->] (b) -- node[right] {\(\mu_* \circ \kappa_*\)} (d);
		\draw[->] (c) -- node[above] {\((\Delta^t_p)_* \circ \otimes\)} (d);
	\end{tikzpicture}
\end{center}

Thus, we conclude:

\begin{lemma}
	The algebraic Tate diagonal is a monoidal natural transformation
	\[
	\Delta^t_p \colon \id_{\cD(\Fp)} \longrightarrow T_p.
	\]
\end{lemma}

\anibal{This kind of narrative structures are ok, but i discurage them in your first paper, stick to a statement followed by its proof.}

\subsection{Relation with the spectral Tate diagonal}

\anibal{You cannot go from defining the derived category of R-modules to just ``recalling'' the infinity category of spectra. I think this needs to be expanded to a whole section}
\anibal{I am not engaging with this since it does not pass the ``smell test''. Your proof at the end of the subsection uses Z. Z does not exist in this paper. Whenever I read it, I suspect that you lost your way.}

Let \({\mathsf{Sp}}\) denote the stable \(\infty\)-category of spectra, and \(\wedge = \wedge_{\bS}\) its symmetric monoidal product.
In [Nikolaus-Scholze], they define the following:

\begin{definition}
	Let \(X\) be a spectrum.
	Let \(T^{\mathsf{Sp}}_p\) denote the functor \(\mathsf{Sp} \to \mathsf{Sp}\) given by
	\[
	T^{\mathsf{Sp}}_p(X) \coloneqq (X^{\wedge p})^{t\cyc_p} = \cofib(Nm \colon (X \wedge \overset{p}{\dots} \wedge X)_{h\cyc_p} \to (X \wedge \overset{p}{\dots} \wedge X)^{h\cyc_p}),
	\]
	the Tate construction of \(X^{\wedge p}\), with \(\cyc_p\)-action given by permuting the factors.
\end{definition}

They prove that it is an exact functor and give

\begin{proposition}
	For any \(F \in \Fun^{\mathrm{Ex}}(\mathsf{Sp},\mathsf{Sp})\), evaluation at the sphere spectrum \(\bS\) induces an equivalence
	\[
	\mathrm{Map}_{\Fun^{\mathrm{Ex}}(\mathsf{Sp},\mathsf{Sp})}(\id_{\mathsf{Sp}},F) \longrightarrow \Hom_{\mathsf{Sp}}(\bS,F(\bS)) = \Omega^\infty F(\bS).
	\]
\end{proposition}

Thus, the space of natural transformations \(\id_{\mathsf{Sp}} \to T_p^{\mathsf{Sp}}\) is equivalent to \(\Hom_{\mathsf{Sp}}(\bS,T_p^{\mathsf{Sp}}(\bS)) = \Omega^\infty T^{\mathsf{Sp}}_p(\bS)\).
This allows them to define

\begin{definition}
	The \defn{spectral Tate diagonal} is the natural transformation \(\Delta_p^{t,\mathsf{Sp}} \colon \id_{\mathsf{Sp}} \to T_p^{\mathsf{Sp}}\) that corresponds to the composition
	\[
	\bS \to \bS^{h\cyc_p} \to \bS^{t\cyc_p} = T_p^{\mathsf{Sp}}(\bS).
	\]
\end{definition}

The derived category \(\cD(\Fp)\) is equivalent to the subcategory \(\sH\Fp\text{-}\mathrm{Mod} \subset \mathsf{Sp}\) of \(\sH\Fp\)-modules, or \(\sH\Fp\)-linear spectra, via the Eilemberg-MacLane functor
\[
\sH \colon \cD(\Fp) \longrightarrow \sH\Fp\text{-}\mathrm{Mod},
\]
that sends a chain complex \(V\) in \(\cD(\Fp)\) to the spectrum \(\sH V = \bigvee_i X_i\), where each \(X_i\) is the Eilemberg-MacLane spectra with \(\pi_i X_i = H_i(V)\) and \(\pi_jX_i = 0\) if \(i \neq j\).\martin{Or is it better to say \(\sH V = \bigvee_i \Sigma^i\sH(H_i(V))\)? The thing is that \(\sH\) is used to define \(\sH\).}
On the other direction, the functor
\[
\pi_* \colon \sH\Fp\text{-}\mathrm{Mod} \longrightarrow \cD(\Fp)
\]
takes a \(\sH\Fp\)-module \(X\) to the chain complex with \(\pi_i(X)\) in degree \(i\) and zero differential.

\(\sH\) is a strong symmetric monoidal functor with respect to the tensor products \(\ot_\Fp\) of \(\cD(\Fp)\) and \(\ot_{\sH\Fp}\) of \(\sH\Fp\text{-}\mathrm{Mod}\), that is, there is an isomorphism
\[
\sH V \ot_{\sH\Fp} \sH V \xra{\cong} \sH(V \ot_\Fp V).
\]
Also, the forgetful functor \(\sH\Fp\text{-}\mathrm{Mod} \to \mathsf{Sp}\) has a lax symmetric monoidal structure given by the canonical projection
\[
\sH V \wedge_{\bS} \sH V \to \sH V \ot_{\sH\Fp} \sH V.
\]
% If \(X\) is a \(\sH\Fp\)-module, then \(T_p^{\mathsf{Sp}}(X)\) is also a \(\sH\Fp\)-module.

Their composition induces a (natural) map that Nikoulaus and Scholze denote by \(\can\)
\[
\can \colon T_p^{\mathsf{Sp}}(\sH V) = (\sH V \wedge_{\bS} \overset{p}{\cdots} \wedge_{\bS} \sH V)^{t\cyc_p} \longrightarrow \left(\sH(V \ot_\Fp \overset{p}{\cdots} \ot_\Fp V)\right)^{t\cyc_p}.
\]
Furthermore, both \(\sH\) and the forgetful functor preserve all limits and colimits, so the Tate construction in spectra is compatible to the algebraic version:
\[
\left(\sH(V \ot_\Fp \overset{p}{\cdots} \ot_\Fp V)\right)^{t\cyc_p} \cong \sH\left((V \ot_\Fp \overset{p}{\cdots} \ot_\Fp V)^{t\cyc_p}\right) = \sH(T_pV),
\]
thus, we will instead write \(\can \colon T_p^{\mathsf{Sp}}(\sH V) \to \sH(T_pV)\).

It is then natural to ask whether the composition of the spectral Tate diagonal with \(\can\) induces the algebraic Tate diagonal.

\begin{proposition}
	Let \(a \in \Fp\).
	\[
	\can \circ a\Delta_p^{t,\mathsf{Sp}} \neq \sH(\Delta_p^t)
	\]
	as maps \(\sH V \to \sH(T_pV)\).
\end{proposition}

\begin{proof}
	We argue by contradiction.
	If they were the same map, then the left square in the following diagram would commute.
	\begin{center}
		\begin{tikzpicture}
			\node (HF1) at (-5,1){\(\sH \Fp\)};
			\node (HF2) at (-5,-1){\(\sH \Fp\)};
			\node (THFF) at (0,1){\((\sH V \wedge_{\bS} \overset{p}{\cdots} \wedge_{\bS} \sH V)^{t\cyc_p}\)};
			\node (HFFT) at (0,-1){\(\sH\left((V \ot_\Fp \overset{p}{\cdots} \ot_\Fp V)^{t\cyc_p}\right)\)};
			\node (HFT1) at (5,1){\((\sH \Fp)^{t\cyc_p}\)};
			\node (HFT2) at (5,-1){\(\sH (\Fp^{t\cyc_p})\)};
			%
			\draw[->] (HF1)--node[above]{\(a\Delta_p^{t,\mathsf{Sp}}\)}(THFF);
			\draw[->] (HF2)--node[above]{\(\sH(\Delta_p^t)\)}(HFFT);
			\draw[->] (THFF)--node[above]{\(m_{\sH \Fp}^{t\cyc_p}\)}(HFT1);
			\draw[->](HFFT)--node[above]{\(\sH (m_{\Fp}^{t\cyc_p})\)}(HFT2);
			\draw[double, double distance=2pt](HF1)--(HF2);
			\draw[->](THFF)--node[right]{\(\can\)}(HFFT);
			\draw[double, double distance=2pt](HFT1)--(HFT2);
		\end{tikzpicture}
	\end{center}
	Since the right square commutes, this implies that the upper row and the bottom row coincide.
	However, it is shown in [Nikolaus-Scholze] that the upper row is not \(\sH\Fp\)-linear unless \(a = 0\), but \(\Delta_p^t\) is nonzero, and we proved in \cref{linearity} that the bottom row is indeed linear, which means that both rows are not the same.
\end{proof}

% We have that the algebraic Tate diagonal is a shadow of the Tate diagonal, in the following sense:
% \begin{proposition}
	% 	For any \(V\) in \(\cD(\Fp)\),
	% 	\[
	% 	\Delta^t_p(V) = \pi_*(\Delta_p(\sH V))
	% 	\]
	% 	as maps \(V \to T_p(V)\) in \(\cD(\Fp)\).
	% \end{proposition}
% \begin{proof}
	% 	It is enough to prove that \(\pi_*(\Delta_p(\sH\Fp))\) is the inclusion map \(\Fp \hookrightarrow T_p(\Fp)\) given by \(1 \mapsto \widehat e_0\).
	% 	By naturality, for any \(f \in \pi_0(\sH\Fp)\) we have a commuting square
	% 	\begin{center}
		% 		\begin{tikzpicture}
			% 			\node (S) at (0,0) {\(\bS\)};
			% 			\node (TS) at (4,0) {\(T_p^{\mathsf{Sp}}(\bS)\)};
			% 			\node (F) at (0,-2) {\(\sH\Fp\)};
			% 			\node (TF) at (4,-2) {\(T_p^{\mathsf{Sp}}(\sH\Fp)\)};
			% 			%
			% 			\draw[->] (S) -- node[right] {\(f\)} (F);
			% 			\draw[->] (TS) -- node[right] {\(T_p^{\mathsf{Sp}}(f)\)} (TF);
			% 			\draw[->] (S) -- node[above] {\(\Delta_p(\bS)\)} (TS);
			% 			\draw[->] (F) -- node[above] {\(\Delta_p(\sH\Fp)\)} (TF);
			% 		\end{tikzpicture}
		% 	\end{center}
	% 	Taking \(f\) to be \(1 \in \pi_0(\sH\Fp) = \Fp\)...\martin{I still have to figure out some details here}
	% \end{proof}
